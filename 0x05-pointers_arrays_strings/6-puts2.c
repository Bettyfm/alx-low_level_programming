#include "main.h"

/**
 * puts2 - function that prints even numbers
 * @str: string
 * no return
 */
void puts2(char *str)
{
	int n;

	for (n = 0; str[i] != '\0'; i++)
	{
		if (i % 2 == 0)
			_putchar(s[i]);
	}
	_putchar('\n');
}
