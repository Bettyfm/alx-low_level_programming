#include "main.h"

/**
 * puts_half - function that prints half of string
 * @str: string input
 * void func no return
 */
void puts_half(char *str)
{
	int i = 0;
	int l;
	int n = 0;

	while (str)
		l++;
	for (n = (l / 2); str[i] != '\0'; n++)
	{
		_putchar(str[n]);
	}
	_putchar('\n');


}
