#include "main.h"
/**
 * _abs - function for absolute value
 *
 * @int: integer for abs
 *
 * Return: absolute value of int
 */
int _abs(int)
{
	int ab;

	if (int < 0)
	ab = int * -1
	return (ab);
	else
	_putchar('int');
	return (0);
}
