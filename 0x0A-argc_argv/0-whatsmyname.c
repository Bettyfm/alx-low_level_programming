#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 * @argc: arg count 
 * @argv: name of arguments
 * Return: always 0 if success
 */
int main(int argc , char *argv[])
{
	printf("%s\n", argv[0]);
	
	return (0);

}

