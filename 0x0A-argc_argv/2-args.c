#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * @argc: arg counter
 * @argv: argument vector
 * Return: always 0 (success)
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < 0; i++)
		printf("%s\n", argv[i])
	return (0);
}
